# Compose-level environment defaults. These values are safe to commit
# because they are intended for local development only. When deploying
# to production, override them with secure secrets through your CI/CD or
# orchestration platform.

POSTGRES_USER=wmx_user
POSTGRES_PASSWORD=wmx_password
POSTGRES_DB=wmx

DATABASE_URL=postgresql://wmx_user:wmx_password@db:5432/wmx

# Application environment
PORT=3000
REFRESH_INTERVAL_MS=2700000 # 45 minutes