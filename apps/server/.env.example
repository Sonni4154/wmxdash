# Environment variables for the API server
#
# DATABASE_URL should point to your Postgres instance. When running under
# Docker Compose, it will use the service name `db` as the hostname.
DATABASE_URL=postgresql://wmx_user:wmx_password@db:5432/wmx

# Port that the Express server listens on. This should match the port mapping
# in your docker-compose.yml file for the `api` service.
PORT=3000